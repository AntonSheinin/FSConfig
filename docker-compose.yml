version: "3.9"

services:
  web:
    image: fsconfig
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      -  redis-stack-server
    ports:
      - "8080:8080"
       
  redis-stack-server:
    image: redis/redis-stack-server:latest
    ports:
      - 6379:6379
  